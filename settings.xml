<settings>
    <servers>
        <server>
            <id>redefantasy</id>
            <username>${env.GITHUB_MAVEN_PUBLISH_USER}</username>
            <password>${env.GITHUB_MAVEN_PUBLISH_PASSWORD}</password>
        </server>
    </servers>
</settings>